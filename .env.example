GEMINI_API_KEY=your_gemini_api_key_here

# Free image generation (default)
# Uses Pollinations (no API key needed). Optional tuning:
# POLLINATIONS_IMAGE_MODEL=flux
# POLLINATIONS_TIMEOUT_S=60
# If you get 401 on some networks, add:
# POLLINATIONS_API_KEY=pk_...  (from enter.pollinations.ai)
